{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Tests",
      "type": "cuda-gdb",
      "request": "launch",
      "program": "${workspaceFolder}/build/benpt_test",
    },
    {
      "name": "Run Frontend",
      "type": "cuda-gdb",
      "request": "launch",
      "program": "${workspaceFolder}/build/render_frontend",
      "initCommands": [
        "cd ${workspaceFolder}/build"
      ]
    }
  ]
}