{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Test",
      "type": "cuda-gdb",
      "request": "launch",
      "program": "${workspaceFolder}/build/test",
      "preLaunchTask": "build"
    }
  ]
}